# ↓ DO NOT EDIT THESE VALUES ↓
migration: 7
locales: [ "cs", "en" ]
# ↑ DO NOT EDIT THESE VALUES ↑
# ----------------------------------------------------------------------------------------
# For descriptions of each option, see the wiki:
# https://github.com/H7KZ/Disenchantment/CONFIG.md
# ----------------------------------------------------------------------------------------

enabled: true

locale: "en"

event-priorities:
  disenchant-click: HIGHEST
  disenchant: HIGHEST
  shatter-click: HIGHEST
  shatter: HIGHEST

# ----------------------------------------------------------------------------------------

logging:
  # NONE = silent | INFO = startup summary | DEBUG = full operation tracing (NMS, adapters, economy, event flow)
  # Set to DEBUG when reporting issues — logs [DEBUG][CATEGORY] lines to the console for every anvil interaction.
  level: INFO
  save-reports: true

# ----------------------------------------------------------------------------------------

disenchantment:
  enabled: true
  disabled-worlds: [ ]
  disabled-materials: [ ]
  enchantments-states: [ ]
  anvil:
    sound:
      enabled: true
      volume: 1.0
      pitch: 1.0
    repair:
      reset: false
      cost: true
      base: 10
      multiply: 0.25
  economy:
    enabled: false
    cost: 100.0
    show-cost: true
    charge-message: true

# ----------------------------------------------------------------------------------------

shatterment:
  enabled: true
  disabled-worlds: [ ]
  enchantments-states: [ ]
  anvil:
    sound:
      enabled: true
      volume: 1.0
      pitch: 1.0
    repair:
      reset: false
      cost: true
      base: 10
      multiply: 0.25
  economy:
    enabled: false
    cost: 100.0
    show-cost: true
    charge-message: true
